import { makeStyles } from '@material-ui/core';

const useStyle = makeStyles({
  breadcrumb: {
    marginTop: '5rem',
    marginBottom: '1rem',
    marginLeft: '3rem',
  },
});

export default useStyle;
